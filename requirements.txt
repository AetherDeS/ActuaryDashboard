# Диапазоны версий: нижняя граница — совместимость с Python 3.10–3.12,
# верхняя — защита от ломающих мажорных релизов.
# Совместимость с numpy 2.x: pandas>=2.2.2 и scikit-learn>=1.5
# (более ранние сборки несовместимы по ABI с numpy 2).
streamlit>=1.31,<2
plotly>=5.18,<6
pandas>=2.2.2,<3
numpy>=1.26,<3
sqlalchemy>=2.0,<3
oracledb>=2.0,<3
scikit-learn>=1.5,<2
